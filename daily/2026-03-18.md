## 20:00 [claude-code]
Session: Cinema Pipeline Full Audit — trio compose fix + 7 infrastructure fixes
- Decision: Полный аудит 6 субъектов pipeline вместо точечных фиксов — выявил CRITICAL баг
- Done: 7 фиксов в 6 файлах (83 ins, 22 del):
  1. CRITICAL: generate-story-brief/runComposer() не вызывал callNanoBanana2Trio — char3 молча терялся
  2. fetchImageAsBase64: timeout 30s + stack overflow fix (uint8ToBase64 вместо spread)
  3. compose-utils: image markers между картинками, temperature 0.65→0.35, continuity label fix
  4. recompose-scene: buildSceneShim castIndex3, trio cost formula (frameCost*2)*N
  5. useNarrativeAssistant: imageUrl3/imageName3 mapping из castIndices[2]
  6. CinemaBuilderPage: handleRecompose отправляет castImageUrl3 + outfitLock3
  7. SceneCard: 3й аватар в UI
- Lesson: Remote (Lovable) реализовал 90% trio архитектуры, но забыл wire в runComposer — ни один тест не поймал, потому что compose-single-scene (Phase 2) работал. runComposer (Phase 1) = параллельный initial compose, был hardcoded на 2 chars
- Next: Deploy 3 edge functions (supabase login нужен), тестировать 3-char story

## 22:30 [claude-code]
Session: Cinema NB2 Compose — Fundamental Quality Refactor
- Decision: Применить проверенный паттерн из generate-sticker к compose pipeline (embedded system text + простые промпты)
- Done: 3 файла (137 ins, 149 del):
  1. CRITICAL: systemInstruction НЕ работает для Gemini image models — embedded как первый text part
  2. buildComposePromptNB2: 600 слов/7 секций → 200 слов/простой промпт
  3. Anchor image убран (причина повтора локаций на всех сценах)
  4. Trio: single-pass 3 ref images вместо 2-step деградации
  5. Solo prompt упрощён аналогично
- Lesson: Gemini image models МОЛЧА игнорируют systemInstruction — доказательство было в нашем же коде (generate-sticker:1378). Простые промпты (~200 слов) работают НАМНОГО лучше чем сложные (~600 слов) с NB2 Flash. Anchor frame = копирование всей сцены вместо только стиля.
- Result: ПЕРВЫЙ УСПЕШНЫЙ 3-char story! Все персонажи узнаваемы, уникальные локации, пользователь впервые собрал видео из всех 5 сцен
- Next: Тюнинг промптов для разных типов объектов (подарки, крафты), видео pipeline отдельно